services:
  - type: web
    name: voicechat
    runtime: node
    plan: free
    branch: main
    autoDeployTrigger: commit
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: MAX_PEERS
        value: "12"
